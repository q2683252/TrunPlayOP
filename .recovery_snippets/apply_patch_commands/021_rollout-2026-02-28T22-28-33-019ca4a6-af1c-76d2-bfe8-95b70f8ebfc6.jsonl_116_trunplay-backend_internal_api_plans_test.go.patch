*** Begin Patch
*** Update File: trunplay-backend/internal/api/plans_test.go
@@
 	id, _ := created["id"].(string)
 	code, _ = postJSON(t, router, "/api/v1/plans/"+id+"/play", nil)
-	// No device_id on plan: StartPlayback returns false -> 500
-	if code != 500 {
-		t.Errorf("play without device: expected 500, got %d", code)
+	if code != 400 {
+		t.Errorf("play without device: expected 400, got %d", code)
 	}
 }
*** End Patch
