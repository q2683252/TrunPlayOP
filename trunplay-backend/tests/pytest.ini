[pytest]
asyncio_mode = auto
testpaths = tests
addopts =
    --cov=src
    --cov-report=html:coverage_html
    --cov-report=term-missing
    -v

[coverage:run]
branch = True
omit =
    src/main.py
    tests/*
